# Xcode / XcodeGen
*.xcodeproj
*.xcworkspace
xcuserdata/
DerivedData/
.build/
.swiftpm/

# XcodeGen cache
.xcodegen/

# Replay rig build output (traces/ has its own .gitignore for raw data + results)
ReplayRig/.derived/

# macOS
.DS_Store

# Python
__pycache__/
*.pyc

# Personal typing sessions — never commit
tools/session-analyzer/sessions/
